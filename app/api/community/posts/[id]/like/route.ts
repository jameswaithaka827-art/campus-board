import { NextRequest,NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { requireSchoolVerifiedUser } from "@/lib/require-user";import { enforceSameOrigin } from "@/lib/request-security";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const o=enforceSameOrigin(req);if(o)return o;const g=await requireSchoolVerifiedUser();if(!g.ok)return g.response;const {id}=await params;const existing=await prisma.communityPostLike.findUnique({where:{postId_userId:{postId:id,userId:g.user.id}}});if(existing)await prisma.communityPostLike.delete({where:{id:existing.id}});else await prisma.communityPostLike.create({data:{postId:id,userId:g.user.id}});const count=await prisma.communityPostLike.count({where:{postId:id}});return NextResponse.json({liked:!existing,count});}
