import { NextRequest,NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { requireActiveVerifiedUser } from "@/lib/require-user";import { enforceSameOrigin } from "@/lib/request-security";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const o=enforceSameOrigin(req);if(o)return o;const g=await requireActiveVerifiedUser();if(!g.ok)return g.response;const {id}=await params;await prisma.communityPostView.upsert({where:{postId_userId:{postId:id,userId:g.user.id}},update:{},create:{postId:id,userId:g.user.id}});const count=await prisma.communityPostView.count({where:{postId:id}});return NextResponse.json({count});}
