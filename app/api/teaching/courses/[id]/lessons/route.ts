import { NextRequest, NextResponse } from "next/server";
import { getServerSession } from "next-auth";
import { authOptions } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { requireTeachingUser } from "@/lib/role-access";
import { assertSameOrigin } from "@/lib/request-security";
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const origin=assertSameOrigin(req);if(!origin.ok)return NextResponse.json({error:origin.reason},{status:403});const {id}=await params;const session=await getServerSession(authOptions);if(!session?.user?.id)return NextResponse.json({error:'Unauthorized'},{status:401});try{await requireTeachingUser(session.user.id)}catch{return NextResponse.json({error:'Forbidden'},{status:403})};const course=await prisma.course.findUnique({where:{id,instructorId:session.user.id}});if(!course)return NextResponse.json({error:'Course not found'},{status:404});const b=await req.json().catch(()=>({}));const title=typeof b.title==='string'?b.title.trim().slice(0,180):'';const summary=typeof b.summary==='string'?b.summary.trim().slice(0,1000):null;const content=typeof b.content==='string'?b.content.trim().slice(0,12000):'';const videoUrl=typeof b.videoUrl==='string'&&b.videoUrl.trim()?b.videoUrl.trim().slice(0,1000):null;if(!title||!content)return NextResponse.json({error:'Title and content are required.'},{status:400});const count=await prisma.lesson.count({where:{courseId:id}});const lesson=await prisma.lesson.create({data:{courseId:id,title,summary,content,videoUrl,position:count,published:false}});return NextResponse.json({lesson},{status:201});}
