import {redirect} from "next/navigation";import {getServerSession} from "next-auth";import {authOptions} from "@/lib/auth";import {prisma} from "@/lib/prisma";import AdminPanel from "./admin-panel";
export default async function AdminPage(){const session=await getServerSession(authOptions);if(!session?.user)redirect('/login');const user=await prisma.user.findUnique({where:{id:session.user.id},select:{name:true,email:true,role:true,accountStatus:true,emailVerificationCompletedAt:true}});if(user?.role!=='admin'||user.accountStatus!=='active'||!user.emailVerificationCompletedAt)redirect('/dashboard');return <main className="min-h-screen"><div className="max-w-7xl mx-auto px-6 py-8"><div className="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-5"><div><p className="text-sm text-brand-300">Control center</p><h1 className="text-4xl md:text-5xl font-bold tracking-tight">James AI Admin</h1><p className="text-white/50 mt-3 max-w-2xl">Your private control room for user access, roles, verification and platform activity.</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] px-5 py-4 min-w-[260px]"><div className="text-xs uppercase tracking-wider text-white/35">Administrator</div><div className="font-semibold mt-2">{user.name||'James AI Admin'}</div><div className="text-white/50 text-sm mt-1 break-all">{user.email}</div><div className="text-xs text-brand-300 mt-2">Verified · Admin</div></div></div><AdminPanel/></div></main>}
