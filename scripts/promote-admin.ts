import {prisma} from "@/lib/prisma";
async function main(){const email=process.argv[2]?.toLowerCase().trim();if(!email)throw new Error("Usage: npm run admin:promote -- your-verified-google-email@gmail.com");const user=await prisma.user.findUnique({where:{email}});if(!user)throw new Error("User not found. Sign in and complete verification first.");if(user.accountStatus!=="active"||!user.emailVerificationCompletedAt)throw new Error("Admin account must be active and email-verified first.");await prisma.user.update({where:{id:user.id},data:{role:'admin'}});console.log(`Promoted ${email} to admin.`)}main().catch(e=>{console.error(e instanceof Error?e.message:e);process.exit(1)}).finally(()=>prisma.$disconnect());
